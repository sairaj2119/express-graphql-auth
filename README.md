# Basic authentication with express graphql and postgres

Steps to run this project:

1. Clone or download the zip
2. Run `npm i` command
3. Setup database settings inside `ormconfig.json` file
4. Run `npm start` command
